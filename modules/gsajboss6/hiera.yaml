---
version: 5
defaults:
  datadir: hieradata
  data_hash: yaml_data

hierarchy:
  - name: "OS Family"
    path: "os/%{facts.os.family}.yaml"

  - name: "environment"
    path: "environment/%{facts.environment}.yaml"

  - name: "common"
    path: "common.yaml"

