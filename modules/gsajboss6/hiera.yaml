---
version: 5

defaults:
  data_hash: yaml_data
  datadir: hieradata

hierarchy:
  - name: "OS family"
    path: "%{facts.os.family}.yaml"
  - name: "OS kernel"
    paths: [ "%{facts.kernel}.yaml", unix.yaml ]
  - name: "common"
    path: common.yaml