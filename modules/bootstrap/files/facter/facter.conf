# must be placed at /etc/puppetlabs/facter/facter.conf and 
# only works with Facter v3.4+
#
# https://docs.puppet.com/facter/3.7/configuring_facter.html

#facts : {
#    blocklist : [ "file system", "EC2" ],
#    ttls : [
#        { "timezone" : 30 days },
#    ]
#}

global : {
# MP: this is a crutch since FACT-42 was not implemented.
# can be deprecated once https://github.com/puppetlabs/facter/pull/1614 gets imported.
    external-dir     : [
        "/opt/puppetlabs/facter/facts.d", 
        "/etc/facter/facts.d",
        "/etc/puppetlabs/facter/facts.d"
    ],
    custom-dir       : [ 
        "/opt/puppetlabs/facter",
        "/etc/facter",
        "/etc/puppetlabs/facter"
    ],
#    no-exernal-facts : false,
#    no-custom-facts  : false,
#    no-ruby          : false
}

#cli : {
#    debug     : false,
#    trace     : true,
#    verbose   : false,
#    log-level : "warn"
#}
